function dateDiff(dateold, datenew) {
    var ynew = datenew.getFullYear(),
        mnew = datenew.getMonth(),
        dnew = dateNew.getDate(),
        yold = dateold.getFullYear(),
        mold = dateold.getMonth(),
        dold = dateold.getDate(),

        diff = datenew - dateold;
}

/*document.write("la différence recherchée est de:")
document.write(dateDiff(dateDeb, dateFin));*/